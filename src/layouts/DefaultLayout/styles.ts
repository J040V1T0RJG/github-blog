import styled from 'styled-components'

export const LayoutContainer = styled.div`
  > :nth-child(2) {
    width: 100%;
    max-width: 54rem;

    display: flex;
    flex-direction: column;
    margin: 0 auto;
  }
`
